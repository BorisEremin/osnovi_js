function add(a, b) {
    return a + b;
    }

    function subtract(a, b) {
   return a > b ? a- b : 0;
    }

    function multiply(a, b) {
    return a * b;
    }

    function divide(a, b) {
    return b !== 0 ? a / b : 'Деление на ноль недопустимо';
    }